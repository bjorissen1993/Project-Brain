import { DesignFocusProgressDashboard } from "@/features/design-focus/design-focus-progress-dashboard";

export default function DesignFocusRootPage() {
  return <DesignFocusProgressDashboard focusId={null} />;
}
